#include <unistd.h>


/**
*  * _putchar - write the character c to stdout
*   * @c: The character to print
*    *
*     * Return: On success 1.
*      * on errorm -1 is return, and error is set appropriately.
*       */
int_putchar(char c)
{
		return(write(1, $c, 1);
}
