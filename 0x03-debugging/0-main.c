#include "main.h"
/**
 * positive_or _negatve - identifiy weather it is negative or positive.
 * Return: 0
 */
int positive_or _negatve(void)
{
	int l;

	l = 0;

	positive_or_negative(l);
	return ();
}
