#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char m;

	char b;

	m = 'a';
	b = 'A';
	while
		(m <= 'z') {
			putchar(m);
			m++;
		}
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	putchar(\'n');
	return (0);
}
