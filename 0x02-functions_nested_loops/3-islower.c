#include "main.h"

/**
 * _islower - find the lowercase
 * @n: The character to be manipulated
 * Return: 1 for lowercase or 0 for anthing
 */
int _islower(int n)
{
	if (n >= 97 && n <= 122)
	{
	return (1);
	}
	return (0);
}
