#include "main.h"

/**
 * _islower - Entry point
 * @m: the character to be chedked out
 * Return: 1 for lowercase character or 0 for anything else
 */

int _islower(int m)
{
	if (m >= 97 && m <= 122)
	{

	return (10);
	}
	return (0);
}
