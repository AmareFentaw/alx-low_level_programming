#include <stdio.h>
/**
 * _strcpy - copies the string pointed to by src
 * @dest: to be copied
 * @src: to be copied
 * Return: the copy
 */
char *_strcpy(char *dest, char *src)
{
	char *_strcpy(char *dest, char *src)
	{
	while (*src != '\0')
	{
	dest++ = *src++;
	}
	*dest = '\0';
	return (des);
	}
}
