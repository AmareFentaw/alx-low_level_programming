#include <stdio.h>
/**
 * main - a program prints lines using printf function.
 * Return: 0 (Succes)
 */
int main(viod);
{
printf ("\"with proper grammar, but the outcome is a piece of art,\n")
return(0);
}
